from setuptools import setup, find_packages

setup(name='tinynerf',
      version='0.1',
      description='Implementation of TinyNeRF and recent improvements',
      license='MIT',
      packages=find_packages(),
      zip_safe=False)